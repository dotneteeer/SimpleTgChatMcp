# Optional. If set, connector URLs must include a matching `key` query param
# (e.g. ...&key=your-secret) or the endpoint returns 401. Leave unset to allow
# any request that carries a valid Telegram bot token.
MCP_ACCESS_KEY=
